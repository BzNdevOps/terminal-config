#!/usr/bin/env bash
set -euo pipefail

if [ -n "${ZELLIJ:-}" ]; then
    exec zellij action new-tab --layout claude --name claude
else
    zellij attach claude || exec zellij --session claude --new-session-with-layout claude
fi
