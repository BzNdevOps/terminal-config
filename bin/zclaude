#!/usr/bin/env bash
set -euo pipefail

if [ -z "${ZELLIJ_SESSION_NAME:-}" ]; then
    unset ZELLIJ ZELLIJ_PANE_ID ZELLIJ_SOCKET_DIR
    exec zellij --session claude --new-session-with-layout claude
fi

exec zellij --layout claude
