#!/usr/bin/env bash
set -euo pipefail

if [ -n "${ZELLIJ:-}" ]; then
    exec zellij action new-tab --layout codex --name codex
else
    zellij attach codex || exec zellij --session codex --new-session-with-layout codex
fi
