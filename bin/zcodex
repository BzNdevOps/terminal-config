#!/usr/bin/env bash
set -euo pipefail

if [ -n "${ZELLIJ:-}" ]; then
    exec zellij action new-tab --layout codex --name codex
else
    /home/bz/bin/zellij attach codex || exec /home/bz/bin/zellij --session codex --new-session-with-layout codex
fi
